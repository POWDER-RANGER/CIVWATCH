# Installation

## Prerequisites

- Node.js 20+ and npm 10+
- Python 3.11+
- Docker and Docker Compose

## Installation Steps

1. Clone the repository
2. Install dependencies: `npm install` (at root or per package)
3. Copy `.env.example` to `.env` and configure required values
4. Start services: `docker compose up -d`
5. Run development servers: `npm run dev`

## Troubleshooting

For issues, see [docs/support.md](../support.md) or open an issue with logs.
